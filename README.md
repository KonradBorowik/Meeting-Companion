# AI based meeting companion

## Description

## Quickstart

- [For Linux (Ubuntu) users] Install ffmpeg

```bash
sudo apt update
sudo apt install ffmpeg -y
```

- Create and activate Python virtuale nvironment

```bash
python -m venv venv
source venv/bin/activate
```